// @loveable:locked - DO NOT REGENERATE THIS FILE
import { defineConfig } from 'vite'
import { tanstackStart } from '@tanstack/react-start/plugin/vite'
import tsconfigPaths from 'vite-tsconfig-paths'

export default defineConfig({
  plugins: [
    tsconfigPaths(),
    tanstackStart({
      server: {
        entry: './src/server.ts',
      },
    }),
  ],
  // Nitro outputs to .vercel/output when preset is 'vercel'
  // This is required for Vercel deployment
  // @ts-ignore - Ignore type layer if your local nitro definition versions mismatch
  nitro: {
    preset: 'vercel',
  },
})